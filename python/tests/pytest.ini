[pytest]
# The suite is randomized by a job of its own, so the ordinary run stays reproducible.
addopts = -p no:randomly
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error
